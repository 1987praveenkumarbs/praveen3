---
 - hosts: localhost
   tasks:
     - name: executing uptime
       shell:
         "uptime"
       register: uptsss
     - name: displaying output
       debug:
         msg: "{{ uptsss }}"


       
