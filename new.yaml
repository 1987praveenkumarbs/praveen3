---
 - hosts: localhost
   tasks:
     - name: executing uptime
       shell:
         "uptime"
       register: uptss
     - name: displaying output
       debug:
         msg: "{{ uptss }}"


       
