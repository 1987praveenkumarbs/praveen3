---
 - hosts: localhost
   ignore_errors: yes
   tasks:
      - name: installings the application
        shell:
          "who|wc -l"
        register: usr_logg

      - name: displaying details
        debug:
          msg: "{{ usr_logg }}"
